server:
  port: **
spring:
  datasource:
    username: ***
    password: ***
    url: jdbc:postgresql://localhost:5432/***
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update